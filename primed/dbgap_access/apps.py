from django.apps import AppConfig


class DbgapAccessConfig(AppConfig):
    """Configuration for the dbgap_access app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "primed_anvil.dbgap_access"
    verbose_name = " dbGaP Access"
